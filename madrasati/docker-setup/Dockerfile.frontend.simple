# Angular Frontend Dockerfile - Using pre-built files
FROM nginx:alpine

# Copy pre-built Angular app
COPY madrasati/madrasati/madrassati/dist/madrassati /usr/share/nginx/html

# Copy nginx config
COPY madrasati/docker-setup/nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
